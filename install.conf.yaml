- defaults:
    link:
      relink: true
      force: true
      stderr: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux/.tmux.conf
    ~/.tmux: tmux/.tmux
    ~/.vim: vim/.vim
    ~/.vimrc: vim/.vimrc
    ~/.zshrc: .zshrc
    ~/.p10k.zsh: .p10k.zsh

- create:
    - ~/.vim/undo-history
